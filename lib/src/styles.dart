/// Style options for HeroIcons. If an option is missing, please open an issue
/// or pull request into the HeroIcons Flutter repository.
enum IconsaxIconStyle {
  bold('bold'),
  broken('broken'),
  bulk('bulk'),
  linear('linear'),
  outline('outline'),
  twotone('twotone');

  const IconsaxIconStyle(this.name);

  final String name;
}
